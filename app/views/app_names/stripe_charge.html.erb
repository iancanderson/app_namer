<h2>This site uses the <em>pay-what-WE-want</em> model.</h2>
<h2>Ralph has decided that you owe: <%= number_to_currency(@charge_amount/100) %></h2>
<div class="hidden">
  <%= form_tag charges_path do %>
    <script src="https://checkout.stripe.com/checkout.js" class="stripe-button hidden"
            data-key="<%= Rails.configuration.stripe[:publishable_key] %>"
            data-allow-remember-me="false"
            data-email="really@tbot.io"
            data-description="Your new app name"
            data-amount="<%= @charge_amount %>"></script>
  <% end %>
</div>

<script>
  var ready;
  ready = function() {
    jQuery("button[type='submit']").click();
  };

  $(document).ready(ready);
  $(document).on('page:load', ready);
</script>
